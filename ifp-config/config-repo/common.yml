management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
#      base-path: /
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: ALWAYS

ifp:
  config:
    common: common config test 1